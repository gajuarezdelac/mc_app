class RequireChangeMaterialModel {
  bool requiresChangeMaterial;
  String weldingId;
  String jointId;

  RequireChangeMaterialModel({
    this.requiresChangeMaterial,
    this.weldingId,
    this.jointId,
  });
}
